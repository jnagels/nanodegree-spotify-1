<resources>

    <!-- Spotify Streamer! -->
    <style name="SS"/>

    <style name="SS.PlayerButton" parent="Widget.AppCompat.Button">
        <item name="android:minWidth">48dp</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:gravity">center</item>
    </style>
</resources>
